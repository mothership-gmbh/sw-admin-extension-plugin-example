import './module/sw-dashboard/sw-dashboard-index';
import './module/sw-product/view/sw-product-detail-base';

// If you want to render the component in a location you need to add the name of the component to the current location.
// This can be done with the sdkLocation store, like the way I made in the init-locations
import './init-locations';
